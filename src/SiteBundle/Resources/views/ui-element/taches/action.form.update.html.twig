{% extends 'base.html.twig' %}

{% block body %}

    <!-- page content -->

    <div class="clearfix"></div>
    <div class="row">
        <div class="col-md-12 col-sm-12 col-xs-12">
            <div class="x_panel">
                <div class="x_title">
                    <h2>modifier une tâche</h2>

                    <div class="clearfix"></div>
                </div>
                <div class="x_content">

                    <div class="form-add-action">
                        <form class="ui form" method="post" action="{{ path('update-task', { 'centrale' : centrale, 'id' : id})  }}">
                            <div class="ui equal width grid">
                                <div class="equal width row">
                                    <div class="column">


                                        <div class="field">
                                            <label>Date d'échéance</label>
                                            <input type="text" class="date add-action" name="cla_echeance"
                                                   placeholder="Date d'échéance">
                                        </div>

                                    </div>

                                    <div class="column">

                                        <div class="field">
                                            <label>Nom de l'action</label>
                                            <input type="text"  name="cla_nom"
                                                   placeholder="{{ task[0]['CLA_NOM'] }}"  maxlength="50">
                                        </div>

                                        <div class="field">
                                            <label>Utilisateur assigné </label>
                                            <select name="cla_us" class="ui dropdown">
                                                {% for us in user %}
                                                    <option value="{{ us.usId }}">{{ us.usPrenom }} {{  us.usNom }}</option>
                                                {% endfor %}

                                            </select>
                                        </div>

                                    </div>

                                </div>

                            </div>
                            <div class="field">
                                <label>Description de l'action</label>
                                <textarea name="cla_desc" rows="2">{{ task[0]['CLA_DESCR'] }}
                                </textarea>
                            </div>

                            <button type="submit" class="fluid positive ui button"> Enregistré</button>
                        </form>
                    </div>


                </div>


            </div>
        </div>
    </div>

    <!-- /page content -->





{% endblock %}